name: skill-creation-guard
version: "0.3.1"
description: Guard Hermes skill_manage calls, audit filesystem-level SKILL.md writes, and enrich every observed skill write with creation origin metadata.
author: HermesAgent
kind: standalone
provides_hooks:
  - pre_llm_call
  - pre_tool_call
  - post_tool_call
  - on_session_start
